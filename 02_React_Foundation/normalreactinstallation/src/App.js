
// These functions are considered as Components.
// In React, components are simply JavaScript functions or classes that return JSX (JSX allows us to write HTML-like code directly within JavaScript), which describes the structure of the UI.
function App() {
  return (
    <h1>Hello React | From Create React App</h1>
  );
}

export default App;
